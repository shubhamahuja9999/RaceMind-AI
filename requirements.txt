# RaceMind AI — dependencies (v1.0)
# Python 3.12+ recommended. Install with: pip install -r requirements.txt

# --- Core simulator ---
# gymnasium[box2d] installs the Box2D physics backend (box2d-py) + pygame + swig.
# Do NOT add a bare `box2d` pin — that distribution has no matching version on PyPI.
gymnasium[box2d]>=1.0.0     # CarRacing-v3 environment (pulls box2d-py, pygame, swig)
pygame>=2.5.0              # rendering, keyboard control and replay
numpy>=1.26.0
pandas>=2.1.0
opencv-python>=4.9.0
matplotlib>=3.8.0

# --- Reinforcement learning (Phase 2) ---
stable-baselines3>=2.3.0
torch>=2.2.0
tensorboard>=2.16.0        # training metrics, logged under runs/
pyyaml>=6.0                # experiment configuration in configs/*.yaml
moviepy>=1.0.3            # encoding evaluation videos (Gymnasium RecordVideo)
imageio-ffmpeg>=0.4.9     # bundled ffmpeg backend for moviepy
